<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pesantren.boardingschool">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_apps"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_apps"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.maps.v2.API_KEY"
            android:value="@string/maps_api_key" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.maps.MapsActivity" />
        <activity android:name=".activity.maps.DetailPesantrenActivity" />
        <activity android:name=".activity.kategori.AliranPesantrenActivity" />
        <activity android:name=".activity.kategori.LokasiPesantrenActivity" />
        <activity android:name=".activity.SettingsActivity" />
        <activity android:name=".activity.about.AboutActivity" />
        <activity android:name=".activity.berita.BeritaActivity" />
        <activity android:name=".activity.berita.DetailBeritaActivity"></activity>
    </application>

</manifest>